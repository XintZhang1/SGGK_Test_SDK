{
  "request_id": "iface_22_offset2d_degenerate",
  "owner": "kernel-dev",
  "target_api": "api_offset2d",
  "sdk_source_refs": [
    "SGGK GeomAlgo/Offset2D/Offset2D.h: Offset2D::Perform"
  ],
  "test_goal": "Generate one 2D offset recipe whose geometry degenerates to a point and matches CrvDegenToPoint.",
  "risk_summary": "Curve degeneration is a typed semantic outcome; treating only the raw success flag as truth creates a false failure or false green.",
  "geometry": {
    "family": "arc2d",
    "target_builder": "one bounded arc2d segment",
    "tool_builder": "none",
    "parameter_notes": "Choose radius and offset distance so the offset collapses to a point; assert exact CrvDegenToPoint and zero result paths."
  },
  "tolerance_focus": [
    "curve_degeneration"
  ],
  "oracles": [
    "offset2d_status",
    "offset2d_result_paths"
  ],
  "expected_behavior": "The SDK returns CrvDegenToPoint, the typed status matches, and the test outcome succeeds with zero result paths.",
  "case_count": 1,
  "run_profile": "smoke",
  "input_assets": {},
  "notes": "Return exactly one flat_recipe object; a matched diagnostic enum is a passing test outcome."
}
